import { ChatContainer } from "@/components/chat/ChatContainer";

export const metadata = {
  title: "Chat — CMDLY",
  description: "Assistant infrastructure piloté par IA",
};

export default function ChatPage() {
  return <ChatContainer />;
}
